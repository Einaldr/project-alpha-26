# nginx.conf
server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        index index.html;
        # This is the magic line for SPAs:
        try_files $uri $uri/ /index.html;
    }
}